/feed.xml /index.xml

# Redirect default Netlify subdomain to primary domain
https://laughing-murdock-60f03f.netlify.com/* https://masalmon.eu/:splat 301!

# These extra rules are required because HTTPS is not forced
http://laughing-murdock-60f03f.netlify.com/* https://masalmon.eu/:splat 301!